{
    "compilerOptions": {
        // -- thêm config baseUrl và paths --
        "baseUrl": ".",
        "paths": {
            "@/*": ["src/*"]
        }
    },
    "exclude": ["node_modules", "dist"]
}
